- content_for :content_header do
  %h1 Groups
- content_for :content_sidebar do
  .block
    %ul
      %li= link_to_memberships
      %li= link_to_invited_memberships  
- if @memberships.count == 0
  You don't belong to any groups.
- else
  .memberships= render @directory.memberships
  = render @directory