require 'spec_helper'

describe RelationshipActivity do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    RelationshipActivity.create!(@valid_attributes)
  end
end
