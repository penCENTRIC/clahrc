require 'spec_helper'

describe MessageActivity do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    MessageActivity.create!(@valid_attributes)
  end
end
