require 'spec_helper'

describe MembershipActivity do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    MembershipActivity.create!(@valid_attributes)
  end
end
