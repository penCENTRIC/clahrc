require 'spec_helper'

describe GroupActivity do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    GroupActivity.create!(@valid_attributes)
  end
end
