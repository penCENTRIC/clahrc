= content_for :content_header do
  %h1= t('new')
- form_for @comment, :url => commentable_comments_path(@page || @post || @topic) do |f|
  = error_messages_for :comment
  %ol
    %li
      = f.text_area :body, :title => ('body_help')
    %li
      = f.submit t('create')