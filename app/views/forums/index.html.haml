- content_for :content_header do
  %h1
    - if @group
      = @group.name_to_s
    - else
      Forums
- if @group
  %h2= t('index')
.forums
  - if @directory.forums.blank?
    This forum does not have any forums yet.
  - else
    = render @directory.forums
= render @directory